module ChinaCityWithGetFilter
end

module ChinaCity
  class << self
    # alias_method :get_without_filte, :get
    # def get(id, options={})
    #   text = get_without_filte(id, options)
    #   text.in?(['市辖区', '县']) ? '' : text
    # end
  end
end
