<% if @exist -%>
  alert('这个孩子已经加入名单，不能重复添加');
<% else -%>
 <% html = escape_javascript(render :partial => "/admin/pair_grant_batch_items/items",
    :locals=>{grants: @grants} ) -%>
 $('#items_list').html('<%=html%>');
<% end -%>
$('#item-tr-<%=@grant.id%>').fadeTo('slow', 0.5).fadeTo('slow', 1.0);
$('#search-btn').click()
$("#items-count").html('<%= @grants.length %>')
