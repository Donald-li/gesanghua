<%= simple_form_for [:admin, @camp_teacher], url: @camp_teacher.new_record? ? admin_project_season_apply_camp_teachers_path(apply_camp_id: @apply_camp.id) : admin_project_season_apply_camp_teacher_path(apply_camp_id: @apply_camp.id), wrapper: :admin_form, html: {class: 'form-horizontal project_form', 'data-parsley-validate': true} do |f| %>
  <%= f.error_notification %>
  <div class="form-body form-horizontal">
    <%= f.input :name, label: '姓名：', placeholder: '', required: true %>
    <%= f.input :id_card, label: '身份证号：', placeholder: '', required: true %>
    <%= f.input :gender, as: :select, collection: ProjectSeasonApplyCampMember.options_for_some_select(:genders, ['male', 'female']), include_blank: false, label: '性别：', placeholder: '' %>
    <%= f.input :nation, as: :select, collection: ProjectSeasonApplyCampMember.options_for_enum_select(:nations), include_blank: false, label: '民族：', placeholder: '' %>
    <%= f.input :phone, label: '联系方式：', placeholder: '', required: true, type: 'phone' %>

    <div class="form-group">
      <label class="col-md-2 control-label">安全协议：</label>
      <div class="col-sm-9">
        <%= webuploader_tag :image_id, label: '安全协议', type: 'camp_protocol_image', asset: @camp_teacher.image, klass: 'single' %>
      </div>
    </div>
  </div>
  <div class="form-actions">
    <div class="row">
      <div class="col-md-offset-2 col-md-9">
        <%= f.button :submit, '提交', class: 'btn blue' %>
        <%= link_to '取消', request.referer, class: 'btn' %>
      </div>
    </div>
  </div>
<% end %>
<script>
  $(function () {
      initImageWebuploader($('#webuploader'));
  })
</script>
