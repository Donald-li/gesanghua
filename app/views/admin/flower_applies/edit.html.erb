<div class="portlet light bordered">
  <div class="portlet-title">
    <div class="caption">
      <i class="icon-note"></i>
      <span class="caption-subject">修改申请</span>
      <span class="caption-helper"></span>
    </div>
    <div class="actions">
      <%= link_to '', class: 'btn btn-xs blue', 'data-no-turbolink' => true do %>
        <!-- <i class="fa fa-download"></i> -->
      <span>操作日志</span>
      <% end %>
    </div>
  </div>
  <div class="portlet-body form">

    <div class="form-body form-horizontal">
      <div class="row">
        <div class="col-md-12">
          <div class="tabbable-line boxless tabbable-reversed">
            <ul class="nav nav-tabs">
              <li class="active">
                <a href="#tab_0" data-toggle="tab" aria-expanded="true"> 申请资料 </a>
              </li>
              <li class="">
                <a href="#tab_1" data-toggle="tab" aria-expanded="false"> 受益学生 </a>
              </li>
              <li class="">
                <a href="#tab_2" data-toggle="tab" aria-expanded="false"> 审核信息 </a>
              </li>
            </ul>
            <div class="tab-content">
              <div class="tab-pane active" id="tab_0">

      <%= simple_form_for [:admin, @project_apply], url: admin_flower_apply_path, wrapper: :admin_form, html: {data: {'parsley-validate': true}, class: 'form-horizontal project_form'} do |f| %>
        <%= f.error_notification %>
        <div class="form-body">
          <%= f.hidden_field :project_id, value: ProjectSeason.flower_project_id %>
          <%= static_form_control '申请编号：', @project_apply.apply_no %>
          <%= f.input :school_id, collection: [@project_apply.school].compact.map {|u| [u.name, u.id]}, label: '申请学校：', autofocus: true, input_html: {class: 'schools-ajax'} %>
          <%= f.input :project_season_id, collection: [@project_apply.season].compact.map {|u| [u.name, u.id]}, label: '申请批次：', autofocus: true, input_html: {class: 'seasons-ajax'} %>
          <div class="form-group">
            <label class="col-md-2 control-label">申请人数：</label>
            <div class="col-sm-9">
              <%= f.text_field :girl_number, placeholder: '女生人数', require: true, type: 'number' %>
              <%= f.text_field :boy_number, placeholder: '男生人数', require: true, type: 'number'%>
            </div>
          </div>
          <%= f.input :describe, label: '申请理由：', required: true %>
          <div class="form-group">
            <label class="col-md-2 control-label">申请图片：</label>
            <div class="col-sm-9">
              <%= webuploader_tag 'apply_image_ids[]', label: '上传申请图片', type: 'project_season_apply_image', asset: @project_apply.images %>
              <!-- <span style="color: red">注意：左起第一张图片为展示第一张图片</span> -->
            </div>
          </div>
          <div style="margin-bottom: 20px;" class="form-group">
            <label class="col-md-2 control-label select required" for="project_category_id">收货地址：</label>
            <div class="col-sm-6">
              <div class="row city-group">
                <div class="col-sm-3">
                  <%= select_tag 'project_season_apply[province]', options_for_select(ChinaCity.list, @project_apply.province.presence), prompt: '省份', class: "form-control city-select" %>
                </div>
                <div class="col-sm-3">
                  <%= select_tag 'project_season_apply[city]', options_for_select(ChinaCity.list(@project_apply.province.to_s), @project_apply.city.presence), prompt: '城市', class: "form-control city-select" %>
                </div>
                <div class="col-sm-3">
                  <%= select_tag 'project_season_apply[district]', options_for_select(ChinaCity.list(@project_apply.city.to_s), @project_apply.district.presence), prompt: '地区', class: "form-control city-select" %>
                </div>
              </div>
              <br>
            </div>
          </div>
          <%= f.input :address, label: '详细地址：', input_html: { data: {}} %>
          <%= f.input :consignee, label: '收货人：', input_html: { data: {}} %>
          <%= f.input :consignee_phone, label: '联系电话：', input_html: { data: {}} %>
        </div>
        <div class="form-actions">
          <div class="row">
            <div class="col-md-offset-2 col-md-9">
              <%= f.button :submit, '提交', class: 'btn blue' %>
              <%= link_to '取消', admin_flower_applies_path, class: 'btn' %>
            </div>
          </div>
        </div>
    <% end %>
  </div>

  <div class="tab-pane" id="tab_1">
    <div class="portlet-body">
      <div class="dataTables_wrapper no-footer">
        <div class="row">
          <div class="col-md-12 col-sm-12">
            <%#= search_form_for @search, url: admin_flower_applies_path, class: 'form-inline' do |f| %>
                <div class="form-group">
                  <%#= f.label :user_nick_name_or_phone_cont, '搜索' %>
                  <%#= f.text_field :user_nick_name_or_phone_cont, class: 'form-control', placeholder: '用户昵称、手机号' %>
                </div>
                <div class="form-group">
                  <%#= f.label :id_eq, '用户ID：' %>
                  <%#= f.text_field :id_eq, class: 'form-control', placeholder: '用户ID' %>
                </div>
                <button type="submit" class="btn btn-default grey">搜索</button>
            <%# end %>
          </div>
          <div class="col-md-6 col-sm-12">
            <div class="dataTables_filter pull-right">
              <%#= link_to '', class: 'btn btn-xs blue', 'data-no-turbolink' => true do %>
                  <i class="icon-plus"></i>
                  <span>新增</span>
              <%# end %>
              <%#= link_to '', class: 'btn btn-xs blue', 'data-no-turbolink' => true do %>
                  <i class="icon-dowmload"></i>
                  <span>导入学生</span>
              <%# end %>
            </div>
          </div>
        </div>
        <div class="table-scrollable">
          <table class="table table-striped table-bordered table-hover" id="sample_2">
            <thead>
            <tr>
              <th>
                格桑花编号
              </th>
              <th>
                身份证号
              </th>
              <th>
                姓名
              </th>
              <th>
                性别
              </th>
              <th>
                民族
              </th>
              <th>
                操作
              </th>
            </tr>
            </thead>
            <tbody>
            <%# @project_applies.each do |apply| %>
                <tr>
                  <td>
                    <%#= apply.id %>
                  </td>
                  <td>
                    <%#= apply.school.try(:name) %>
                  </td>
                  <td>
                    <%#= apply.season.try(:name) %>
                  </td>
                  <td>
                    <%#= l apply.created_at %>
                  </td>
                  <td>
                    <%#= apply.try(:girl_number) + apply.try(:boy_number) %>人
                  </td>
                  <td>
                    <%#= link_to '', 'data-no-turbolink' => true, class: 'btn btn-xs blue' do %>
                        <i class="fa fa-edit"></i> 修改
                    <%# end %>
                    <%#= link_to '', class: 'btn btn-xs red', method: :delete, data: {confirm: '确定要删除吗？'} do %>
                        <i class="fa fa-trash-o"></i> 删除
                    <%# end %>
                  </td>
                </tr>
            <%# end %>
            </tbody>
          </table>
        </div>
        <div class="row">
          <div class="col-md-5 col-sm-12">
            <div class="dataTables_info" id="sample_4_info"><%#= paginate_info(@project_applies) %></div>
          </div>
          <div class="col-md-7 col-sm-12">
            <%#= paginate @project_applies, theme: '' %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="tab-pane" id="tab_2">
    <div class="portlet-body">
      <div class="table-scrollable">
        <table class="table table-bordered table-hover">
          <tbody>
          <tr>
            <th width="72px">
              审核状态
            </th>
            <th>
              <span id="check_state"><%= @audit.enum_name(:state) %></span>
            </th>
          </tr>
          <tr>
            <th>
              审核评语
            </th>
            <th>
              <span id="check_comment"><%= @audit.comment %></span>
            </th>
          </tr>
          </tbody>
        </table>
      </div>
    </div>
    <div class="form-actions">
      <div class="row">
        <div class="col-md-offset-2 col-md-9">
          <!-- <a class="btn blue" data-toggle="modal" data-target="#myModal_update" href="#responsive"> 修改审核信息 </a> -->
          <a class="btn blue" data-toggle="modal" data-target="#myModal_create" href="#responsive"> 审核 </a>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
</div>
</div>
</div>
</div>
</div>

<% if false %>
<div class="modal fade" id="myModal_update" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
<div class="modal-dialog" role="document">
<div class="modal-content">
<%= simple_form_for [:admin, @project_apply, @audit], url: update_audit_admin_flower_apply_path, remote: true, wrapper: :admin_form, html: {'data-parsley-validate': true} do |f| %>
<%= f.error_notification %>
<div class="modal-header">
<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
<h4 class="modal-title" id="myModalLabel">审核信息</h4>
</div>
<div class="modal-body">
<div class="portlet-body form">
<div class="form-body form-horizontal">
<%= f.hidden_field :project_season_apply_id, value: params[:id] %>
<%= f.input :state, as: :radio_buttons, wrapper: :inline_inputs, collection: Audit.options_for_some_select(:states), label: '审核：', required: true%>
<%= f.input :comment, label: '评语：', placeholder: '请填写评语' %>
</div>
</div>
</div>
<div class="modal-footer">
<button type="button" class="btn btn-default" data-dismiss="modal">关闭</button>
<button type="submit" class="btn btn-primary" >提交审核</button>
</div>
<% end %>
</div>
</div>
</div>
<% end %>

<div class="modal fade" id="myModal_create" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
<div class="modal-dialog" role="document">
<div class="modal-content">
<%= simple_form_for [:admin, @project_apply, @new_audit], url: create_audit_admin_flower_applies_path, remote: true, wrapper: :admin_form, html: {'data-parsley-validate': true} do |f| %>
<%= f.error_notification %>
<div class="modal-header">
<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
<h4 class="modal-title" id="myModalLabel">审核信息</h4>
</div>
<div class="modal-body">
<div class="portlet-body form">
<div class="form-body form-horizontal">
<%= f.hidden_field :project_season_apply_id, value: params[:id] %>
<%= f.input :state, as: :radio_buttons, wrapper: :inline_inputs, collection: Audit.options_for_some_select(:states), label: '审核：', required: true%>
<%= f.input :comment, label: '评语：', placeholder: '请填写评语' %>
</div>
</div>
</div>
<div class="modal-footer">
<button type="button" class="btn btn-default" data-dismiss="modal">关闭</button>
<button type="submit" class="btn btn-primary" >提交审核</button>
</div>
<% end %>
</div>
</div>
</div>
    <script type="text/javascript">
        $(function(){
            //    表单验证
            $(".project_form").html5Validate(function(){
                $(this).find('[type="submit"]').attr('disabled','disabled');
                this.submit();
            });
            // 异步加载学校列表
            $('.schools-ajax').select2({
                language: "zh-CN",
                placeholder: '请选择学校',
                allowClear: true,
                ajax: {
                    url: "/selects/schools",
                    dataType: 'json',
                    delay: 250,
                    data: function (params) {
                        return {
                            q: params.term, // search term
                            page: params.page
                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 1;
                        return {
                            results: $.map(data.items, function (item) {
                                return {
                                    text: item.name,
                                    id: item.id
                                }
                            }),
                            pagination: {
                                more: (params.page * 30) < data.total_count
                            }
                        };
                    },
                    cache: true
                }
            });

            // 异步加载批次列表
            $('.seasons-ajax').select2({
                language: "zh-CN",
                placeholder: '请选择批次',
                allowClear: true,
                ajax: {
                    url: "/selects/flower_seasons",
                    dataType: 'json',
                    delay: 250,
                    data: function (params) {
                        return {
                            q: params.term, // search term
                            page: params.page
                        };
                    },
                    processResults: function (data, params) {
                        params.page = params.page || 1;
                        return {
                            results: $.map(data.items, function (item) {
                                return {
                                    text: item.name,
                                    id: item.id
                                }
                            }),
                            pagination: {
                                more: (params.page * 30) < data.total_count
                            }
                        };
                    },
                    cache: true
                }
            });

            //多图上传
            initMultiImageWebuploader('.upload-wrap');
            $('.upload-list').viewer();
        })
    </script>

  </div>
</div>
