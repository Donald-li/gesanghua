<div class="person-body-right pull-right">

  <div style="display: block;">
    <%= render '/layouts/shared/platform_school_projects' %>
    <% if @applies.present? %>
      <div class="school-apply-pair-table-co">
        <div class="school-apply-pair-table">
          <p>申请项目名称</p>
          <p>申请状态</p>
          <p>操作</p>
        </div>
        <% @applies.each do |apply| %>
          <div class="school-apply-pair-tables">
            <p>申请时间：<%= apply.created_at.strftime("%Y-%m-%d %H:%M")%> <span>申请编号：<%= apply.apply_no %></span></p>
            <div class="school-apply-pair-table-cont">
             <div>
               <p class="school-apply-pair-tit"><%= apply.apply_name %></p>
               <!-- <p class="school-apply-pair-f">申请人：张老师</p> -->

             </div>
             <div class="border-l border-r">
               <%= apply.enum_name(:pair_state) %>
             </div>
             <div>
               <button><%= link_to '查看申请详情', platform_school_apply_pair_path(apply) %></button>
               <% if apply.waiting_upload? %>
                <button><%= link_to '提交名单', child_list_platform_school_apply_pair_children_path(apply) %></button>
               <% elsif apply.pair_complete? %>
                <button><%= link_to '查看名单', platform_school_apply_pair_children_path(apply, state: 'pass') %></button>
               <% end %>
             </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="donation-cont-non">
        <div class="donation-co-no-c">
          <p>您的学校暂未有<%= @current_project.name %>项目申请</p>
        </div>
        <%= link_to new_donate_path do %>
          <div class="donation-co-no-but">去捐助</div>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="project-foot">
    <%#= paginate @applies, theme: :tiny %>
  </div>
</div>
