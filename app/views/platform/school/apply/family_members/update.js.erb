<% if @result %>
<% html = j render 'item', member: @member %>
$('#member-<%= @member.id.to_s %>').replaceWith('<%= html %>')
$('#myModal').modal('hide')
layer.msg('保存成功', {
    icon: 1,
    offset: 't c'
});
<% else %>
layer.msg('保存失败', {
    icon: 2,
    offset: 't c'
});
<% end %>