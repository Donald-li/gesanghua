# 系统后台
admin:
  menus:
    admin_main:
      actions:
        'admin/mains': all
    # 平台管理
    admin_platform:
      menus:
        admin_platform_content:
          menus:
            admin_content_adverts:
              actions:
                'admin/adverts': all
            admin_content_articles:
              actions:
                'admin/articles': all
            admin_content_supports:
              actions:
                'admin/supports': all
            admin_content_pages:
              actions:
                'admin/pages': all
    # 运营管理
    admin_operation:
      menus:
        admin_user:
          menus:
            admin_user_county_users:
              actions:
                'admin/county_users': all

    admin_system:
      menus:
        admin_article_category:
          actions:
            'admin/article_categories': all
        admin_project_template:
          actions:
            'admin/project_templates': all

    # 管理员列表
    admin_administrator:
      menus:
        admin_administrators:
          actions:
            'admin/administrators': all
        admin_audits:
          actions:
            'admin/audits': all
        admin_platform_content:
          menus:
    # 财务
    admin_finance:
      menus:
        admin_audit_reports:
          actions:
            admin/audit_reports: all
        admin_financial_reports:
          actions:
            admin/financial_reports: all
        admin_fund_categories:
          actions:
            'admin/fund_categories': all
    # 项目管理
    admin_project:
      menus:
        admin_pair:
          menus:
            admin_pair_project:
              actions:
                'admin/pairs': all
            admin_pair_applies:
              actions:
                'admin/pair_applies': all
            admin_pair_lists:
              actions:
                'admin/pair_lists': all
